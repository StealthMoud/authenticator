{
  "name": "authenticator-extension",
  "version": "2.0.0",
  "private": true,
  "description": "Local-first Manifest V3 authenticator extension",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:syntax": "for file in background.js js/*.js lib/vaultSync.js; do node --check \"$file\" || exit 1; done",
    "check": "npm run check:syntax && npm test"
  }
}
